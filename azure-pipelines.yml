trigger:
  branches:
    include:
      - '*'

pool:
  name: Default

steps:
  - task: UseNode@1
    inputs:
      version: '20.x'
      checkLatest: true
  
  - script: |
      npm install
    displayName: 'Install dependencies'
  
  - script: |
      npm run lint
    displayName: 'Lint'